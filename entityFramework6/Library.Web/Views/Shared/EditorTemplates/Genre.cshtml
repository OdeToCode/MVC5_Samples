@using Library.Entities
@using Library.Web.Helpers

@{   
    var type = ViewData.ModelMetadata.ModelType;
    var items = EnumHelper.GetItems(type, (int)Model);       
}

@Html.DropDownList("", items)